{
  "name": "noosh-ai-mcp-test",
  "version": "1.0.0",
  "type": "module",
  "description": "NooshAI Chatbot E2E Testing with Playwright MCP",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "build": "tsc",
    "mcp:server": "node dist/mcp/chatbot-validator-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^25.0.2",
    "dotenv": "^16.3.0",
    "typescript": "^5.3.0"
  }
}
